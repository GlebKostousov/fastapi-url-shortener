name: Python checks

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master
    paths:
      - ".github/workflows/**"
      - "url-shortener/**"

jobs:

  run-check:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code # скачиваем код в раннер
        uses: actions/checkout@v5
      - uses: actions/setup-python@v5 # скачиваем питон из кеша гитхаб
        with:
          python-version-file: '.python-version' # вот отсюда берем версию питона

      - name: Install Black⬛
        run: |
          python -m pip install --upgrade pip
          pip install "black==25.1.0"
      - name: Black # проверяем блеком
        run: |
          black --check --diff ./

      - name: Setup UV # установка UV
        uses: astral-sh/setup-uv@v5
        with:
          pyproject-file: 'pyproject.toml' # файл с настройками
          enable-cache: true
      - name: Install the project # установка зависимостей
        run: uv sync --locked --all-extras --dev

      - name: Run ruff --dif # запуск ruff c показом ошибок
        uses: astral-sh/ruff-action@v3
        with:
          args: 'check --diff'
          version-file: "pyproject.toml"
      - name: Run ruff # запуск ruff стандарт, чтобы показать что все ок
        uses: astral-sh/ruff-action@v3
        with:
          version-file: "pyproject.toml"

      - name: run mypy # запуск mypy через UV
        run: uv run mypy url-shortener

  run-tests:
    runs-on: ubuntu-latest
    needs:
      - run-check
    services:
      redis:
        image: redis
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 6379:6379
    steps:
      - name: Checkout code
        uses: actions/checkout@v5
      - uses: actions/setup-python@v5
        with:
          python-version-file: '.python-version'

      - name: Setup UV
        uses: astral-sh/setup-uv@v5
        with:
          pyproject-file: 'pyproject.toml'
          enable-cache: true

      - name: Install the project
        run: uv sync --locked --all-extras --dev

      - name: Run python pytest
        run: uv run pytest url-shortener --cov-report=json:coverage.json --cov=url-shortener --cov-report=xml:coverage.xml
        env:
          TESTING: 1
          REDIS_PORT: 6379

      - name: Prepare coverage percentage
        run: |
          export EXTRACTED_COVERAGE=$(python -c 'import json;print(json.load(open("coverage.json"))["totals"]["percent_covered_display"])')
          echo "TOTAL_COVERAGE=$EXTRACTED_COVERAGE" >> $GITHUB_ENV
          echo "### Tests coverage: {$EXTRACTED_COVERAGE}%" >> $GITHUB_STEP_SUMMARY

      - name: Update coverage badge
        uses: schneegans/dynamic-badges-action@v1.7.0
        with:
          auth: ${{ secrets.GIST_TOKEN }}
          gistID: 8ccc9117a4a81627856cb5d379113e0f
          filename: coverage.json
          label: Coverage
          message: ${{env.TOTAL_COVERAGE}}%
          minColorRange: 70
          maxColorRange: 90
          valColorRange: ${{ env.TOTAL_COVERAGE}}

      - name: Upload coverage report to Codecov
        uses: codecov/codecov-action@v5
        if: ${{ !github.event.act }}
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          files: coverage.xml
          dry_run: ${{ env.ACT }}
          fail_ci_if_error: 'true'
